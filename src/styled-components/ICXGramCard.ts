import styled from "styled-components";

const ICXGramCard = styled.div`
  background-color: ${({ theme }) => theme.colors.lightDark};
  color: ${({ theme }) => theme.colors.lightTextColor};
  padding: 12px;
  border-radius: 15px;
`;

export default ICXGramCard;
